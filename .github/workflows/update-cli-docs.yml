name: 'Update CLI docs with latest release from CLI repo'

on:
  workflow_dispatch:
    inputs:
      cli_branch:
        description: 'Branch or tag from the CLI repo'
        required: true
      commit_author:
        description: 'Name to use for the commit author'
        required: true
      commit_author_email:
        description: 'Email to use for the commit author'
        required: true
      commit_message:
        description: 'Commit message for the docs update'
        required: true

jobs:
  update:
    name: 'Pull changes from the CLI repo and update CLI docs'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    defaults:
      run:
        shell: bash

    steps:
      - name: Generate token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.TEMPORAL_CICD_APP_ID }}
          private-key: ${{ secrets.TEMPORAL_CICD_PRIVATE_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: cli
          ref: ${{ github.event.inputs.cli_branch }}
          submodules: recursive
          persist-credentials: true
          token: ${{ steps.generate_token.outputs.token }}

      - name: Checkout docs repo
        uses: actions/checkout@v4
        with:
          repository: temporalio/documentation
          persist-credentials: true
          token: ${{ steps.generate_token.outputs.token }}
          path: docs

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.22'

      - name: Generate CLI docs
        run: |
          cd cli/temporalcli/internal/cmd/gen-docs
          go run .

      - name: Publish generated docs to documentation repo
        run: |
          set -ex

          git config user.name "${{ github.event.inputs.commit_author }}"
          git config user.email "${{ github.event.inputs.commit_author_email }}"
          
          BRANCH_NAME="cli-update-docs-${{ github.event.inputs.cli_branch }}"
          echo "$BRANCH_NAME"
          # Remove the "/merge" suffix if it exists TODO: Is this still needed?
          BRANCH_NAME=${BRANCH_NAME%/merge}

          cd docs
          git checkout -b $BRANCH_NAME
          cp ../cli/temporalcli/docs/*.mdx docs/cli/
          git add .
          git commit -m "${{ github.event.inputs.commit_message }}"
          git push origin "$BRANCH_NAME"

          gh pr create \
            --body "Autogenerated PR from https://github.com/temporalio/cli" \
            --title "${{ github.event.inputs.commit_message }}" \
            --head "$BRANCH_NAME" \
            --base "main"
