/**
 * Declarative marker — never mounted. See WalkthroughStep.js. Authored as a
 * child of a `<WalkthroughStep>` to add one entry to the Events ledger, or
 * (with only a `divider` prop) a phase-boundary divider within it.
 */
export default function WalkthroughEvent() {
  return null;
}
