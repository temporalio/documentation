/**
 * Declarative marker — never mounted. See WalkthroughStep.js. Authored as a
 * child of a `<WalkthroughStep>` to add one entry to the Commands ledger.
 */
export default function WalkthroughCommand() {
  return null;
}
